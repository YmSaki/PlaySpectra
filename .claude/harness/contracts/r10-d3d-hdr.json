{
  "task_id": "r10-d3d-hdr",
  "dod_checks": [
    "cmake --build layer/build --target vr_agent_layer succeeds cleanly",
    "HDR E2E with HELLO_XR_HDR=1: on runtimes enumerating R16G16B16A16_FLOAT the capture returns ok=true, format:10, tonemapped:true, Vulkan-identical colorConversion wording, non-flat PNG; on runtimes not enumerating 16F the mjs prints an explicit SKIP (runtime does not enumerate 16F) without failing rc",
    "Regression: default-env D3D11 and D3D12 integration tests 17/17 on both metasim and monado (4 runs), plus MSAA 18/18 re-runs (metasim, D3D11 and D3D12) proving R08/R09 paths intact",
    "git diff confined to capture_common.{h,cpp}, capture_d3d11.cpp, capture_d3d12.cpp, setup_helloxr_msvc.sh, integration_hello_xr.mjs (+ harness docs); capture_vulkan.cpp has ZERO diff"
  ],
  "rubric_refs": [],
  "non_goals": [
    "R17 D3D11 TYPELESS acceptance (next task); 16-bit TYPELESS stays an explicit error on both D3D backends (no guessed decode)",
    "R15 error-JSON unification",
    "Vulkan backend changes of any kind (its HDR path stays as-is)",
    "other wide formats (R16G16B16A16_UNORM / R32G32B32A32_FLOAT / A2B10G10R10) — explicit error stays, matching Vulkan"
  ],
  "risk_flags": [],
  "review": { "profile": "runtime", "max_iterations": 3 },
  "workflow": {
    "phases": ["inv", "plan", "work", "review", "ship"],
    "parallel_groups": []
  },
  "status": "approved",
  "approval_reason": "No risk flags. Behavior change (D3D HDR capture from explicit error to Vulkan-shaped tonemapped output) user-approved in backlog.md Deferred header (R08->R09->R10->R17). Decode logic is the proven Vulkan implementation shared via pixel_convert/capture_common."
}
