{
  "task_id": "r17-d3d11-typeless",
  "dod_checks": [
    "cmake --build layer/build --target vr_agent_layer succeeds cleanly AND vr_agent_test builds and passes 14/14",
    "TYPELESS E2E with HELLO_XR_TYPELESS=1 on D3D11: runtimes enumerating R8G8B8A8_TYPELESS capture ok=true with format:27 and non-flat PNG; non-enumerating runtimes produce an explicit mjs SKIP without failing rc",
    "Regression: default-env D3D11 17/17 on metasim and monado, MSAA (metasim D3D11) 18/18, HDR (metasim D3D11) 18/18",
    "git diff confined to layer/src/capture_d3d11.cpp, scripts/setup_helloxr_msvc.sh, scripts/integration_hello_xr.mjs (+ harness docs); zero diff in D3D12/Vulkan backends and capture_common"
  ],
  "rubric_refs": [],
  "non_goals": [
    "R16 dxgi_formats.h shared header (stays an Open move-only task; the small mapping duplication introduced here is folded by R16)",
    "16-bit TYPELESS acceptance (ambiguous interpretation, explicit error stays -- L69)",
    "D3D12-side TYPELESS E2E via hello_xr patch (layer already accepts it; out of scope)",
    "R15 error-JSON unification"
  ],
  "risk_flags": [],
  "review": { "profile": "runtime", "max_iterations": 3 },
  "workflow": {
    "phases": ["inv", "plan", "work", "review", "ship"],
    "parallel_groups": []
  },
  "status": "approved",
  "approval_reason": "No risk flags. Behavior change (D3D11 TYPELESS from explicit error to accepted) user-approved in backlog.md Deferred header (R08->R09->R10->R17, final item). Rule mirrors the existing reviewed D3D12 backend."
}
