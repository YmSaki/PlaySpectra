# v1.0.0 完成定義（DoD） — VR版Playwright

> **⚠️ 本文書は 2026-07-14 時点の凍結記録（当時の layer 直結アーキテクチャ前提）。現行の完成定義と現況は
> `backlog.md` 冒頭「現況スナップショット」が正**。以下は当時のまま残しており、現況と食い違う主な点:
> D3D11/D3D12 キャプチャは「⬜未対応」→ **実装済み・3API 各20/20=60/60 検証済み**(2026-07-22)。
> GAP-08 非CAフォールバックの「runtime E2E 不可」→ **誤り**(`PLAYSPECTRA_DISABLE_CA=1` で 15/15、README 記載)。
> 「実アプリ検証は XR 有効化後」→ **G1 で達成済み**(VRAppDummyGame/Godot 4.7 で 24/24、2026-07-25)。
> MCPツール列挙は廃止方向の TS 版の語彙 → 現行実働は `tools/playspectra_mcp.py` の **13ツール**。
> `openxr_agent_layer.cpp` は現 `layer/src/layer_entry.cpp` ほかへ分割済み。

**前提**: v1.0.0 では下記を**全部実装**する。「機能なし／未対応」の区別は**着手順とリリース前の暫定状態の扱い**を決めるだけで、範囲外を意味しない。
- **機能なし** = 能力そのものが存在しない（未着手の capability）。
- **未対応** = 機能は在るが、あるパターンをまだ捌けない。**今は明示エラー/文書化（silently-brokenは禁止）**、v1.0.0までに実装。
- 判別: 「その機能自体が無いか（機能なし）／機能は在りこのケースだけ捌けないか（未対応）」。D3D11/D3D12は「capture機能は在る・そのバインディングパターンが未対応」＝**未対応**。

## ✅ 実装済み
- 入力注入: ボタン/アナログ/active（CA拡張）、コントローラー姿勢 位置+向き（レイヤー内 xrLocateSpace 上書き）、頭部姿勢（レイヤー内 xrLocateViews/xrLocateSpace(VIEW) 上書き）
- キャプチャ: **Vulkan** カラー → PNG（xrEndFrame readback）
- MCPツール: vr_status / vr_input / vr_active / vr_screenshot(eye:left/right/dominant/both) / vr_set_controller / vr_clear_controller / vr_set_hmd / vr_clear_hmd / vr_move / vr_reset / vr_recenter / vr_point_at / vr_look_at / vr_click / vr_wait / vr_haptics / vr_capture_sequence
- 制御チャネル（TCP NDJSON）、堅牢性（型不正でアプリを落とさない・終了ハング無し 等の確定バグ修正、E2E検証済み）

## ⬜ 機能なし（能力を新設する）※3件を並列worktreeで実装・統合・E2E検証済み(2026-07-13)
- [x] **vr_actions** — アクション発見。E2E: hello_xrのactionSet/actionをバインド先パス付き(select/click@simple, squeeze/value@touch)でダンプ確認。※現在値/isActiveは静的registry優先で未実装(正直注記済み、xrGetActionStateはセッションスレッド要)。
- [x] **observe/act 橋渡し(vr_view)** — 両目pose+FOV+写像手順(view=eye pose逆, 射影=FOV半角)を返す。E2E: available:true確認。将来 raycast_from_pixel/射影行列へ拡張余地。
- [x] **深度キャプチャ(withDepth)** — XrCompositionLayerDepthInfoKHR検出→Vulkan深度readback→線形化→16bitグレースケールPNG+メタ実装。E2E: hello_xrは深度未提出のため `available:false`+正直注記まで確認。**available:true経路(実深度PNG)は深度提出アプリで要E2E**（未検証ギャップ）。
  - 全3件: 3並列worktreeエージェントがblind実装→統合で全コンパイル成功→E2E動作。**各機能のHaiku暗黙知+メタ認知レビューは未実施(次工程)**。

## ⬜ 未対応（機能は在る／パターンを埋める。今は明示エラー、v1.0.0までに実装）
※2026-07-13 第2バッチ: キャプチャを「単一dispatch＋APIごと別TU backend(平坦インターフェース)」へ再設計(scaffold 4348b9c)し、
D3D11/D3D12/MSAA/HDR/aim/velocity/null-subaction を5worker並列(互いに素なファイル→衝突0)で実装。全5ブランチ
クリーンマージ→**統合ビルド一発green**→メタ認知レビュー全pass(must_fix 0、D3D12のみconcerns=要runtime検証)。
- [x] **D3D11 キャプチャ**（capture_d3d11.cpp: STAGING tex+CopySubresourceRegion+Map/RowPitch de-pad+BGRA swizzle+lodepng。ビルド+レビュー済。**runtime E2E未**: MinGW hello_xrはVulkan専用のためD3D11実行アプリ要）
- [x] **D3D12 キャプチャ**（capture_d3d12.cpp: DIRECT allocator+RT→COPY_SOURCE barrier+256整列footprint+fence同期。ビルド+レビュー済。**runtime E2E未**同上。RENDER_TARGET初期state仮定は実機要確認)
- [x] **aim pose 上書き**（openxr_agent_layer.cpp: grip→aimオフセット合成、恒等毒化回避。ビルド+レビュー済。**runtime E2E未**: aim使用アプリ要）
- [x] **velocity ゼロ化**（XrSpaceVelocity/Velocities を静止=0+VALID。非const helper+nullガード。ビルド+レビュー済）
- [x] **MSAA解決 + HDR(RGBA16F)** フォーマット（capture.cpp Vulkanパス: vkCmdResolveImage中間画像+dst座標ゼロ化、RGBA16Fトーンマップ。ビルド+レビュー済。**runtime E2E未**: MSAA/HDRswapchainアプリ要）
- [x] **null-subactionPath** アクション空間（openxr_agent_layer.cpp: bindingパス接頭辞から手推定、ロック配置是正。ビルド+レビュー済）
- [x] **マルチスレッドエンジン安全性**（GAP-07, 471784e）。縮小スコープで実装: 各hookの function-local static next-PFN を単一 LayerDispatch に集約し xrCreateApiLayerInstance 毎に再構築・xrDestroyInstance でクリア(instance再生成での stale PFN バグ解消)。CA/util系PFNも畳み込み破棄時クリア。lazy-init 安全性は create時一括解決で自然消滅。capture.cpp: FreeVulkanResources に in-flight fence 待ち。**handle→instanceレジストリは新設せず**(北極星の単一instanceモデル維持=メタ認知の過剰スコープ是正に従う)。メタ認知レビュー: 正しさ・スコープとも合格。
- [x] **非CAランタイムのボタン/アナログ in-layer フォールバック**（GAP-08, 5438500 + 71a9a161是正）。レイヤ内で action-system をエミュレート: xrSyncActions で永続ラッチ→ xrGetActionState{Bool,Float,Vector2f} 上書き(集約規則: bool OR/float abs-max/vec2 最大magnitude)+ xrGetCurrentInteractionProfile 上書き + xrPollEvent で InteractionProfileChanged 合成。CA経路は g_ca_enabled 短絡で不変。メタ認知 must_fix(ロック順序=g_action_mutex保持中の ToPath)を2フェーズ化で是正済み。**runtime E2E未**: Meta sim は CA 対応のため本経路は非CAランタイム+アプリで要検証。

## ⬜ 横断（品質・検証・文書）
- [x] per-instance dispatch化（セッション再作成・stale next 対策）※GAP-07に統合・完了(471784e)
- [x] **統合テスト（実OpenXRアプリでエンジン非依存面を検証）**（GAP-10, a751805）。実アプリ ArcadiaMetaverses(Godot) は現状OpenXR未初期化(開発中/ユーザー確認済み)のため、ユーザー判断で標準準拠の hello_xr を対象に scripts/integration_hello_xr.mjs + scripts/integration_test.sh を追加。**14/14 PASS**: (a)profile/binding傍受(actions attached+boundPaths)、(b)xrLocateViews上書きがランタイム応答に反映(head注入で view |Δ|=1.487)、(c)sync往復(grab注入→haptic 0→64)、(d)非退化キャプチャ(PNGデコード, distinctColors=5)。エンジン固有作業ゼロ=North Star遵守。※真の第2エンジン(Godot/Vulkan)検証は ArcadiaMetaverses が XR 有効化された後に別途(要ユーザー合図)。
- [x] **README**（日本語、セットアップ/使い方）※実装状況に忠実な日本語READMEを作成(1b79208)

## ✅ GAP-07/08 検証状況メモ（2026-07-14）
- **統合ビルド green**（LayerDispatch刷新 + 非CAフォールバック追加後、openxr_agent_layer.cpp/capture.cpp コンパイル+リンク成功）。
- **GAP-07 runtime E2E PASS (5/5)**: hello_xr -g Vulkan + Meta XR Sim で **CA経路(=E2E検証済みマイルストーン)が dispatch 全面刷新後も無傷**であることを確認。status: api:Vulkan/381フレーム/projection:true/2views、screenshot(dominant) 実Vulkanカラー4937B、pose set+pose_get 往復(x:0.2,y:-0.2,z:-0.5,qw:1 = 入力注入が g_dispatch.syncActions→CA 経由で動作)、pose後2枚目 4263B(app生存)。**全hookが g_dispatch 経由で正しく解決されることを実証**。※初回はウォームアップ前(framesObserved:0)に撮ると「no projection layer」になる既知タイミング(コード問題ではない)—listen後~6秒待てば安定。
- **GAP-08 は runtime E2E 不可**（Meta sim は CA 対応で g_ca_enabled=true → フォールバック経路は短絡され走らない）。build + メタ認知レビュー(must_fix=ロック順序を2フェーズ化で是正済み)で検証。非CAランタイム+アプリでの実走は既知の未検証ギャップ(D3D11/D3D12/MSAA/HDR と同列)。
- **メタ認知レビュー**: GAP-07 は正しさ・スコープ規律とも合格(handle→instanceレジストリ非新設=過剰スコープ再発なし)。GAP-08 は must_fix 1件(g_action_mutex 保持中の ToPath/stringToPath)を2フェーズ化で是正、concern(vec2集約=最大magnitude化、counter reset)も反映。

## ✅ 検証状況メモ（2026-07-13）
- **統合ビルド green**（capture.cpp/capture_d3d11.cpp/capture_d3d12.cpp/openxr_agent_layer.cpp 全コンパイル+リンク、BUILD_EXIT=0）。
- **runtime E2E PASS (5/5)**: hello_xr -g Vulkan + Meta XR Sim でクリーン検証。
  - status: Vulkan/95フレーム観測/projection=true/2views
  - **Vulkanカラーキャプチャ回帰なし**: screenshot ok=true・PNG 4937B。**MSAA worker(capture.cpp)の新フィールド `msaaResolved:false, colorConversion:"direct 8-bit (no tonemap)"` が出力**＝MSAA/HDRコードが統合され単サンプルRGBA8で正しくdirectパスに分岐・正直報告。
  - **INP: pose注入+pose_get往復** (x:0.2,y:-0.2,z:-0.5,qw:1)、pose後もapp生存(2nd shot 4263B)＝INP変更でクラッシュ無し。
  - 制御チャネル port52700 は session到達後~4秒でLISTEN。
- 検証の教訓: 当初 `RPC failed: Deadline Exceeded` を環境ブロックと誤診したが、実際はフロントエンド起動時の**過渡的エラーで即回復**(直後 Successfully requested connection→FOCUSED到達)。末尾の `adb cannot be reached` は Android実機転送用ADBでヘッドレスsimに無関係。接続失敗の実因は**繰り返しテストで溜まったゾンビhello_xr(Bashバックグラウンドの孤児ハンドル残存・実体は死亡)とタイミング**。プロセス衛生を正して単発クリーン実行で通った。→ [[hello-xr-getchar-stdin-eof]] と同系の「自分のハーネスを先に疑う」教訓。
- **未カバー(既知ギャップ・専用アプリ要)**: D3D11/D3D12 runtime(MinGW hello_xrはVulkan専用)、MSAA解決/HDRトーンマップの実行経路(hello_xrは単サンプルRGBA8のため分岐は確認済だが解決コード自体はMSAA/HDRアプリが必要)、aim/velocity/null-subactionの個別挙動(hello_xrはgrip使用。ただし共有する剛体pose注入経路はE2E済)。

## 規律チェック（各タスク着手時に確認）
1. これは「機能なし」か「未対応」か（silently-broken にしていないか＝未対応なら明示エラー/文書化されているか）。
2. 全体最適か（既存機構の拡張か／並行システムを作っていないか）。
3. 機械的検証があるか（効果を検証、成功コードを信じない）。
