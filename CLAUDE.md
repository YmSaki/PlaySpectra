# CLAUDE.md

## 対話言語

ユーザーとの対話（応答・報告・質問）は**日本語**で行う。
（コード・コメント・コミットメッセージ等の成果物は、既存コードの慣習に合わせる。）

## このプロジェクトの核となる要求（north star）

これは「VR版Playwright」である。それ以上でもそれ以下でもない。

- Playwright がブラウザにキーボード/マウスの合成入力を注入し、画面（DOM/スクリーンショット）を観察するのと同じように、
  VR-HMD とVRコントローラーを触ったのと同じ入力をVRアプリに注入し、画面を観察できればよい。
- 画面取得は既定で利き目カラー画像+深度マップだが、深度マップは「出してもいい」(nice-to-have) であって必須仕様ではない。
  生の左右ステレオも締め出さない（`eye` パラメータで選択可能にする）。
- エンジン非依存（OpenXR API レイヤー方式）は正しい選択。Unity/Unreal/ネイティブが共通で読む
  「入力キュー + フレームバッファ」に相当する層に位置するため、Playwrightがブラウザエンジンの内部実装を
  気にしないのと同様、個々のエンジン固有プラグインを書く必要がない。

## 【重要】検証済みと未検証を混ぜない（2026-07-18、ユーザー指摘で恒久化）

同一セッション内で3回再発した失敗: 学習知識由来の主張（SteamVR の役割割当挙動、oculus_touch
偽装のバインディング解決、Monado の CA 対応）を、実測事実と同じ確度で仕様書・README・実装に
書き込んだ。うち1つ（Monado CA）は実測と真逆だった。

**規律**:
- 文書・設計・実装に主張を書く前に、必ず「実測/一次ソース由来の**事実**」か「学習知識由来の
  **仮説**」かを分類する。仮説は必ず 仮説ラベル+検証方法 付きで書く（例: H1〜H3 方式、
  .claude/steamvr-driver-plan.md「実機との共存」節）。
- 仕様・アーキテクチャの決定は事実の上にだけ置く。仮説の上に置く場合は「検証装置としての
  暫定実装」と明示し、実測後に決定へ昇格させる。
- README 等の外向き文書は ✅実装+自動テスト済み / 🟡実機で部分検証済み / 📋設計・開発中 の
  3段階で検証境界を明示する。「できること」と「できる予定」を混ぜない。
- 判別法: その主張を裏付ける **このリポジトリ内の実測ログ・テスト結果・一次ソース** を
  指させるか？ 指せなければ仮説。

## 過去に起きた勘違いとその原因（再発防止用）

過去のセッションで、設計レビュー用の Plan エージェントに実装計画のレビューを依頼したところ、
OpenXR仕様への忠実性を追求した結果、Unreal既定のD3D12対応やUnity統合テストなど、
コアループ（入力注入+画面取得）の完成に必須ではない項目まで多数プランに書き加えられた。
これをそのまま「必須スコープ」として全部プランに取り込んでしまい、ユーザーから
「Unreal/D3D12を挟むのではなく、キーボード操作と同じ動作をするVR版Playwrightを作れという話だったはずだが」
と指摘された。

**原因**: (1) 元の依頼文で深度マップは「出してもいい」という許容・任意のニュアンスだったのに、
これを見落として必須要件のように扱った。(2) 外部レビュー（Plan agent）が仕様網羅性を優先するのは
その agent の役割上当然だが、それをコアスコープを絞り込む立場のこちらが無批判に全部プランへ
畳み込んでしまい、局所最適（個々の指摘は正しい）が全体最適（元の依頼の単純さ）を侵食した。

**今後の指針**:
- 実装は常に「コアループ（入力注入+画面キャプチャ）が動くこと」を最優先とする。
- 設計レビューで出てきた指摘は、「コアループに必須」か「将来の拡張（milestone 2以降、または
  やらなくてもよいnice-to-have）」かを都度分類し、後者をタスクリストの主軸に混ぜない。
- ユーザーの依頼文の助動詞・許容表現（「してもいい」「〜でもよい」等）は要件の強さを表すシグナルなので、
  プランに落とし込む際に強制要件へ格上げしない。

## 【重要】スコープの2軸を取り違えない（over-scope と under-scope の両方が罪）

上の「勘違い」を反省した際、逆方向にもミスをした。「D3D12対応はUnreal固有だから後回し(nice-to-have)」
と誤って切り捨てたところ、ユーザーから
「フォーマットが違えば対応できません、という不完全な状態で作るのか。キーボードだがEnterキーしか
入力できません、と一緒だ」と正しく叱られた。

スコープには**別々の2軸**があり、どちらを侵しても不完全品になる:

1. **OpenXR 抽象化レベルの完全性（コア必須・「キーボードの全キー」）**:
   - 入力注入の全経路（全アクション種別・両手・頭部姿勢）
   - 画面キャプチャの**全グラフィックスAPI = D3D11 / D3D12 / Vulkan**
   これらは `XrGraphicsBindingD3D11KHR` / `...D3D12KHR` / `...VulkanKHR` という **OpenXR自身が定義する
   構造体**であり、エンジン固有ではない。1つでも欠けると「そのフォーマットのVRアプリは撮れません」
   という不完全品になり、VR版Playwrightを名乗れない。**絶対に後回し(nice-to-have)にしてはいけない。**

2. **OpenXR 抽象化の下（エンジン固有・スコープ外）**:
   - Unreal専用プラグインコード、Unity専用プラグインコード、特定エンジンの統合テストを
     必須マイルストーンに置くこと。これは避ける。

**判別法**: 「それは OpenXR API の構造体・関数で表現される普遍的な話か、特定エンジンの内部実装の話か」。
前者ならコア必須（全部やる）。後者ならスコープ外（やらない/後回し）。
D3D11/D3D12/Vulkan は前者なので**全部コア必須**。「Unreal のプロジェクト設定をこう変える」は後者。

nice-to-have（やらなくてもよい）に分類してよいのは、ユーザーが明示的に許容表現で「出してもいい」と
言ったもの（例: 深度マップ）だけ。API完全性を勝手に nice-to-have に格下げしない。

## 進行中の実装について

詳細な設計・タスク分解は `.claude/` 配下（セッションのplanファイル）を参照。
本ファイルは「何を見失ってはいけないか」を記録する場所であり、実装の詳細はここに書かない。

## リポジトリ運用の注意（.claude/ の git 追跡）

`.claude/` はユーザーの **global gitignore 対象**。よって `.claude/` 配下に設計書・ルール等を新規作成しても
既定では追跡されない。設計書を作ったら **`git add -f` で明示追跡する**（放置すると repo に入らず、追跡済みの
README 等からのリンクが切れる）。追跡文書は非追跡パスへリンクしない。
— 出典: 2026-07-22（PlaySpectra 設計書5点が未追跡で README リンク切れ→git add -f で是正、526de7d9）
